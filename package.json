{
  "name": "catan-translations",
  "displayName": "Catan Translations",
  "version": "1.0.0",
  "devDependencies": {
    "arr-flatten": "^1.0.1",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-clean": "^0.3.2",
    "gulp-concat": "^2.6.0",
    "gulp-exec": "^2.1.2",
    "gulp-inject": "^4.1.0",
    "gulp-plumber": "^1.1.0",
    "gulp-sass": "^2.3.2",
    "gulp-sourcemaps": "^2.2.0",
    "gulp-typescript": "^3.0.2",
    "gulp-webserver": "^0.9.1",
    "typescript": "^2.0.6",
    "xlsx": "^0.8.0"
  },
  "dependencies": {
    "angular": "*",
    "angular-animate": "*",
    "angular-aria": "*",
    "angular-material": "*",
    "bootstrap": "4.0.0-alpha.5",
    "fuse.js": "^2.5.0",
    "jquery": "^3.1.1"
  },
  "scripts": {
    "start": "gulp",
    "build": "gulp clean && gulp build",
    "build:noclean": "gulp build",
    "clean": "gulp clean",
    "clean:all": "gulp cleanall",
    "cordova": "gulp clean && gulp cordova",
    "cordova:copy": "gulp mobile-clean && gulp mobile-copy",
    "cordova:clean": "gulp mobile-clean",
    "alternative": "gulp clean && gulp alternative",
    "alternative:copy": "gulp alternative-clean && gulp alternative-copy",
    "alternative:clean": "gulp alternative-clean",
    "webserver": "gulp webserver",
    "all": "gulp cleanall && gulp build && gulp alternative-copy && gulp mobile-copy"
  }
}
